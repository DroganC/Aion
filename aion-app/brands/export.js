/**
 * Active brand id for build and dev.
 * Change `brand` to switch product naming + icons (see brands/<id>/).
 *
 * Icons: put files under brands/<id>/icons/ (app.icns / app.ico / app.png /
 * app_dev.png / login.png). Missing files fall back to aion-app/resources/.
 * Optional override: BRAND=<id> (CI one-off only).
 */
module.exports = { brand: 'wework' };
